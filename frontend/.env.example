# Local development: Optionally point directly to Flask backend server.
# Leave empty in production on Vercel to use relative /api routing with Vercel rewrites/proxies.
VITE_API_BASE_URL=http://localhost:5000
